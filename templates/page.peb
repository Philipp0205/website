{% include "header.peb" %}

    <div class="container-narrow">
      {% if content.title is defined %}
        <header class="page-header">
          <h1>{{ content.title }}</h1>
        </header>
      {% endif %}

      <div class="post-content">
        {{ content.body | raw }}
      </div>
    </div>

{% include "footer.peb" %}